// 앱 전역 설정 값

// 딥링크 스킴 (app.json 의 scheme 과 일치해야 한다)
export const APP_SCHEME = 'couplemap';

// 초대 코드 유효 시간 (시간 단위)
export const INVITE_EXPIRY_HOURS = 48;

// 초대 코드 길이
export const INVITE_TOKEN_LENGTH = 8;
